<!--
Shows the results of a search

Receives its data as an array of Course objects, sorted by relevance. For the initial POC, this means that only the course with the matching course number will be returned. However, once search-by-name is implemented (by zombies), the list snould be all the classes that match
-->